{
  "name": "codecare",
  "displayName": "CodeCare",
  "description": "Gently reminds developers to hydrate, stretch, and relax while coding.",
  "icon": "icon.png",
  "version": "0.0.1",
  "publisher": "DevSilva",
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codecare.helloWorld",
        "title": "CodeCare: Hello World"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "CodeCare",
      "properties": {
        "codecare.enableReminders": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable CodeCare reminder popups."
        },
        "codecare.reminderInterval": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "description": "Set the reminder interval in minutes."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.101.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}